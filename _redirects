# Netlify and Cloudflare Pages both read this file.
#
# Path level rule only. Anything that is not a real file falls back to the
# holding page rather than a 404, which is what you want while the site is
# one page.
#
# Domain level redirects are NOT done here. See DEPLOY.md, they are set per
# platform: Netlify does it from the primary domain setting, Cloudflare Pages
# from a Redirect Rule, bunny.net from an Edge Rule, Apache from .htaccess.

/*    /index.html   200
